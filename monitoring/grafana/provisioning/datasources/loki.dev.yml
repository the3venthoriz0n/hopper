apiVersion: 1

datasources:
  - name: Loki
    type: loki
    uid: loki
    access: proxy
    url: http://dev-hopper-loki:3100
    isDefault: false
    editable: true
    jsonData:
      maxLines: 1000
      volumeEnabled: true
      httpHeaderName1: "X-Scope-OrgID"
      httpHeaderValue1: "fake"
