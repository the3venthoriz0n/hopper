# .env.example - Template for environment variables
# Copy this to .env.dev or .env.prod and fill in your values
#

# IMPORTANT: After setting up your domain, register these redirect URIs with your OAuth providers:
#   - Google Cloud Console: https://${DOMAIN}/api/auth/youtube/callback
#   - TikTok Developer Portal: https://${DOMAIN}/api/auth/tiktok/callback
#   - Instagram Developer Portal: https://${DOMAIN}/api/auth/instagram/callback
#   - For local dev, also add: http://localhost:8000/api/auth/youtube/callback (if needed)
#
# GDPR COMPLIANCE: Register the Data Deletion Instructions URL with your OAuth providers:
#   - Instagram Developer Portal (Settings > Basic > User Data Deletion):
#     Select "Data Deletion Instructions URL" and enter: ${FRONTEND_URL}/delete-your-data
#   - TikTok Developer Portal (if required):
#     Enter: ${FRONTEND_URL}/delete-your-data
#   Note: This page is served by the frontend React app, not the backend

# Cloudflare Access
CLOUDFLARE_ACCESS_AUD_TAG=your-aud-tag-here
CLOUDFLARE_ACCESS_TEAM_DOMAIN=your-team.cloudflareaccess.com

# Environment
ENVIRONMENT=production
LOG_LEVEL=INFO

# Domain Configuration
DOMAIN=your.domain.here
FRONTEND_URL=https://your.domain.here
BACKEND_URL=https://your.domain.here

# Google OAuth (YouTube)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_PROJECT_ID=your-project-id

# TikTok OAuth
TIKTOK_CLIENT_KEY=your-tiktok-client-key
TIKTOK_CLIENT_SECRET=your-tiktok-client-secret

# Instagram OAuth (Direct Instagram Login - No Facebook Page Required)
# Note: Requires Instagram Business or Creator account
# Get credentials from: https://developers.facebook.com/apps (configure Instagram Login)
INSTAGRAM_APP_ID=your-instagram-app-id
INSTAGRAM_APP_SECRET=your-instagram-app-secret

# Database
POSTGRES_PASSWORD=change-this-to-a-secure-password
# DATABASE_URL for local scripts (outside Docker)
# Dev: postgresql://hopper:${POSTGRES_PASSWORD}@localhost:5433/hopper
# Prod: postgresql://hopper:${POSTGRES_PASSWORD}@localhost:5432/hopper
DATABASE_URL=postgresql://hopper:${POSTGRES_PASSWORD}@localhost:5433/hopper

# Security - Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-encryption-key-here

# Stripe Configuration
# Get these from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...  # Use sk_live_... for production
STRIPE_PUBLISHABLE_KEY=pk_test_...  # Use pk_live_... for production
# Get webhook secret from: https://dashboard.stripe.com/webhooks (after creating webhook endpoint)
STRIPE_WEBHOOK_SECRET=whsec_...
# Get pricing table ID from: https://dashboard.stripe.com/pricing-tables (after creating pricing table)
STRIPE_PRICING_TABLE_ID=prctbl_...  # Pricing table ID (starts with 'prctbl_')
STRIPE_API_VERSION=2024-11-20.acacia  # Optional, defaults to this version

# Remote Server Configuration (for rsync file syncing)
# Use 'make sync' to sync your local code for hot reload
# 
# For local Windows path (WSL): Leave REMOTE_HOST empty or set to "local"
#   Example: REMOTE_PATH=/mnt/y/Misc/_DevRemote/hopper
#   Or use Windows format: REMOTE_PATH=Y:\Misc\_DevRemote\hopper
#
# For remote SSH: Set REMOTE_HOST to your server
#   Example: REMOTE_HOST=your-server-ip-or-hostname
#   Example: REMOTE_PATH=/root/_dev/hopper
REMOTE_HOST=
REMOTE_USER=root
REMOTE_PATH=/mnt/y/Misc/_DevRemote/hopper

# Cloudflare R2 Storage Configuration
# Get these from: https://dash.cloudflare.com (R2 > Manage R2 API Tokens)
# Create separate buckets and access keys for dev and prod environments
R2_ACCOUNT_ID=your-r2-account-id
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_BUCKET_NAME=your-r2-bucket-name
R2_ENDPOINT_URL=https://your-account-id.r2.cloudflarestorage.com
R2_PUBLIC_DOMAIN=