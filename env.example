# .env.example - Template for environment variables
# Copy this to .env.dev or .env.prod and fill in your values
#

# IMPORTANT: After setting up your domain, register these redirect URIs with your OAuth providers:
#   - Google Cloud Console: https://${DOMAIN}/api/auth/youtube/callback
#   - TikTok Developer Portal: https://${DOMAIN}/api/auth/tiktok/callback
#   - Instagram/Facebook Developer Portal: https://${DOMAIN}/api/auth/instagram/callback
#   - For local dev, also add: http://localhost:8000/api/auth/youtube/callback (if needed)
#
# GDPR COMPLIANCE: Register the Data Deletion Instructions URL with your OAuth providers:
#   - Facebook/Instagram Developer Portal (Settings > Basic > User Data Deletion):
#     Select "Data Deletion Instructions URL" and enter: https://${DOMAIN}/delete-your-data
#   - TikTok Developer Portal (if required):
#     Enter: https://${DOMAIN}/delete-your-data

# Cloudflare Access
CLOUDFLARE_ACCESS_AUD_TAG=your-aud-tag-here
CLOUDFLARE_ACCESS_TEAM_DOMAIN=your-team.cloudflareaccess.com

# Environment
ENVIRONMENT=production
LOG_LEVEL=INFO

# Domain Configuration
DOMAIN=your.domain.here
FRONTEND_URL=https://your.domain.here
BACKEND_URL=https://your.domain.here

# Google OAuth (YouTube)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_PROJECT_ID=your-project-id

# TikTok OAuth
TIKTOK_CLIENT_KEY=your-tiktok-client-key
TIKTOK_CLIENT_SECRET=your-tiktok-client-secret

# Instagram OAuth (Instagram Graph API via Facebook Login for Business)
# Note: Requires Instagram Business or Creator account linked to a Facebook Page
# Use your Facebook App ID and Secret (not Instagram-specific credentials)
FACEBOOK_APP_ID=your-facebook-app-id
FACEBOOK_APP_SECRET=your-facebook-app-secret

# Database
POSTGRES_PASSWORD=change-this-to-a-secure-password

# Security - Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-encryption-key-here

# Remote Server Configuration (for rsync file syncing)
# Use 'make sync' to sync your local code for hot reload
# 
# For local Windows path (WSL): Leave REMOTE_HOST empty or set to "local"
#   Example: REMOTE_PATH=/mnt/y/Misc/_DevRemote/hopper
#   Or use Windows format: REMOTE_PATH=Y:\Misc\_DevRemote\hopper
#
# For remote SSH: Set REMOTE_HOST to your server
#   Example: REMOTE_HOST=your-server-ip-or-hostname
#   Example: REMOTE_PATH=/root/_dev/hopper
REMOTE_HOST=
REMOTE_USER=root
REMOTE_PATH=/mnt/y/Misc/_DevRemote/hopper